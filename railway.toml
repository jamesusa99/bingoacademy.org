[build]
builder = "NIXPACKS"
# Deps installed by nixpacks.toml; only build here (avoids double npm ci → EBUSY)
buildCommand = "npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
